# Estado de las Especificaciones (Specs)

A continuación se resume el estado real, sincronizado y auditado de las 15 especificaciones arquitectónicas del proyecto Datanestiq.

| Spec | Fase real | Deuda abierta principal | Próximo hito |
|------|-----------|-------------------------|--------------|
| **001 (Elevación Premium)** | Implementado en Astro (producción) | Componentes UI en Vanilla, dependencias Tailwind pendientes de compilación completa. | Refactor nativo a Astro Components (`<Button>`, `<Card>`). |
| **002 (Microexperiencias IA)** | ✅ Consume la taxonomía enriquecida en las 5 islas (chatbot guiado **0 llamadas**, `problems` conservado). **Chatbot texto-libre resiliente**: `chat.php` con **failover Groq→DashScope→Gemini** + **grounded en servicios**. **+ Fix Duplicación Subtítulos Chatbot (AUDITADO)**: `SYSTEM_PROMPT` en `public/api/chat.php` exige formato de viñeta único `- [Nombre](/soluciones/slug): Explicación` y prohíbe explícitamente repetir el nombre del servicio fuera del enlace (sintaxis PHP validada con `php -l`). **+ Fix Latencia Buscador Semántico**: caché a nivel de módulo en `public/worker.js` (<1s búsquedas subsecuentes). | Afinamiento de umbrales del Buscador Semántico y streaming SSE. | Refinar threshold NLP. |
| **003 (Taxonomía Servicios)** | ✅ Content Collections validadas + 10 páginas sector. **+ Migración SSOT `/casos-de-exito` AUDITADA**: 5 sectores (`finanzas`, `retail`, `salud`, `publico`, `seguros`) enriquecidos con `roiCases`. `/casos-de-exito.astro` lee dinámicamente de `sectorsCorpus.json` vía `flatMap` (0 casos hardcodeados en `.astro`), renderizados mediante el componente unificado `<IllustrativeRoiCase />` con badge **`[EST] Escenario Ilustrativo`** y tooltip transparente de honestidad §2. Heading en páginas de sector ajustado a *"Escenarios de Impacto Económico Potencial"*. | Múltiples fuentes unificadas vía pre-build JSON | Expandir a otr| **004 (Metodología)** | 🔴 **SUPERSEDED_BY: 010**. Marcador formal de desuso añadido en `specs/004/spec.md`. El pipeline LangGraph en Python jamás se construyó; su objetivo (agentes que redactan y mantienen el sitio) se sirve formalmente con el patrón single-shot controlado y multi-destino de la Spec 010 (`docs-generator.mjs`). | Ninguna (deuda cerrada por superseded). | Cerrada formalmente por superseded. |
| **005 (OpenWiki Privada)** | ✅ CONSOLIDADA en Spec 010 (su motor propio evolucionó a `docs-generator.mjs`, target `wiki`) — NO archivada como fallida | Ninguna (deuda migrada a la 010) | Cerrada por superseded; ver Spec 010. |
| **006 (Ecosistema Astro)** | ✅ Implementado + **tooling de validación REAL** (eslint + `@lhci/cli` instalados, ya no son stubs `echo`). | Baseline ESLint = 149 errores/130 warnings por limpiar (no bloqueante). Lighthouse configurado pero **no produjo scores** (error `EPERM` de Windows en Temp). | Limpiar deuda de lint; correr Lighthouse en entorno sin EPERM. |
| **007 (Expansión Multi-Página)**| Implementado + **Breadcrumbs Schema JSON-LD** (BreadcrumbList) en BaseLayout. **+ Vocabulario Controlado de Tags (AUDITADO)**: 18 tags canónicos (4 roles C-Level + 4 sectores + 10 temas), normalización del frontmatter `tags:` en los 16 `.md` del blog. Reducción de ruido SEO de 45 a **16 páginas de tag estáticas `/blog/tag/[tag]`** de alta señal (ej: `/blog/tag/sector-publico` con 7 posts, `/blog/tag/gobierno-del-dato` con 5 posts). Layout editorial sin `glass-card`. Total **54 páginas estáticas**. | Falta la **auditoría Lighthouse real** (bloqueada por `EPERM` de Windows; el tooling ya existe). | Correr Lighthouse en entorno sin EPERM y reportar scores. |
| **008 (Headless WordPress)** | ⏸️ En pausa por decisión del usuario. Código ya remediado: `remote_extract.py` lee de `.env` y está gitignored. | 🔴 **CRÍTICO (acción del usuario):** rotar la contraseña SSH de IONOS — sigue expuesta en el historial de git. Falta CPTs y API REST. | Rotar SSH en IONOS; configurar endpoints WP cuando se retome. |
| **009 (OpenWiki LangChain)** | 🔴 [ARCHIVADA] Bloqueada por cuota de free tier. | Agotaba limits por el flujo del agente | Pivotó a Spec 010 (Generador Multi-Destino) |
| **010 (Generador Multi-Destino)** | 🟢 **COMPLETA Y FORMALIZADA CON PATRÓN DIAMANTE (2026-08-01).** 4 targets (`wiki`, `agents`, `skills`, `blog`) ejecutados en paralelo con `Promise.all` (`npm run docs:sync`). `specs/010/plan.md` y `specs/010/tasks.md` formalizados en SDD. | Ninguna bloqueante. | Mantener operativamente para sincronización de documentación. |
| **011 (Enriquecimiento Taxonomía)** | ✅ **Fase 2 + Fase 3 completas y AUDITADAS**: 10/10 sectores, 6/6 pilares, 7 personas específicas por industria. Localización de roles (`roleEquivalents` F-11) y Schema Zod intactos. **Fix de Deuda Pre-existente (2026-08-01):** `personasCorpus.json` generado en `src/data/personasCorpus.json` sin el campo `contentAngles`. Islas (`HeroRoleLine`, `Chatbot`, etc.) actualizadas a `personasCorpus.json`. Verificado `grep contentAngles dist/` = 0. | Cero fugas de data interna al cliente. | ✅ Pipeline completo y cerrado. |: `personas.json` aún envía `contentAngles` al cliente. | ✅ Pipeline completo (F3 → fundación → Spec 013). |
| **013 (Conversión Consultiva por Rol × Sector)** | ✅ **COMPLETA y AUDITADA en navegador (spec-marco + 3 fases).** `specs/013-.../spec.md`. **Motor A1–A9:** `userContext` store persistido, `<ContextChips>` (4 chips: Sector Público, Finanzas/CFO, **Datos/CDO**, Estrategia/CEO), `<ConsultativeCTA>` dinámico, reuso de `semanticHighlight`, formulario vía `open-chatbot`→`chat.php`, `<IllustrativeRoiCase>` `[EST]`. **+ Localización de Roles UI (Auditada)**: `<Chatbot>` (botones de rol y saludos heredados), `<HeroRoleLine>` (mapeo `mapSectorSlugToId` slug→id y `getLocalizedRoleTitle` sin paréntesis dobles), y `<SolutionsByRoleAndIndustry>` consumen `roleEquivalents` de forma **0-LLM** (0 fetch). | ✅ Modelo del Business Case Estimator REFINADO y AUDITADO en navegador. | Sumar roles nuevos (CISO/COO) como fases del motor 013 cuando lleguen sus insumos. |
| **012 (Fábrica de Contenido del Sitio)** | ✅ **Fase 1 + Fase 2 operativas y AUDITADAS**. F1: colección `pages` (Zod+`loader:glob`), catch-all `[...slug].astro`, `--target=page` (draft-gating verificado). F2: **home data-driven** (`home.md`→`index.astro` vía `getEntry`+defaults, `set:html` correcto, **paridad verificada** tras fix `\n`→`<br/>`); **sin `/home` duplicada**; 5 tipos de sección nuevos; workflow `content-pr.yml` (secrets + PR, sin auto-merge). Islas intactas, build 25 páginas. | Menor: `pagina-muestra.md` (demo F1) sigue publicada. | Fase 3 opcional: proponer cambios de home vía el flujo PR; más rollout. |
| **014 (Ciclo de Vida del Lead — Mini-CRM SQLite)** | ✅ **DESPLEGADO Y AUDITADO EN PRODUCCIÓN (2026-08-01).** Live en subdominio `https://app.datanestiq.com`. BD SQLite en `/homepages/37/d4298973101/htdocs/app/secure_leads/crm.sqlite` (modo WAL, FKs, fuera del document root public, `.htaccess` Deny from all verificado 403). Endpoints `save_wizard.php` funcionando, panel `/admin/` funcional. **IP whitelist deshabilitado a propósito (acceso password-only por IP dinámica del usuario); riesgo aceptado y compensado con bcrypt + rate-limit + HTTPS + sesión segura. Ver `tech_debt.md`.** Default de `auth.php` restablecido a fail-closed (`127.0.0.1,::1`). WordPress en `datanestiq.com` 100% intacto. | Riesgo de superficie expuesta en `/admin/` compensado con bcrypt + rate-limit 5/5min. | Revisar si el usuario adquiere IP fija o VPN de egreso fijo. |
| **015 (Agendador de Citas)** | ✅ **DESPLEGADO Y AUDITADO EN PRODUCCIÓN (2026-08-01).** Live en subdominio `https://app.datanestiq.com`. Entidad `appointments` creada en el mismo `crm.sqlite`. Endpoints `availability.php` (zona horaria `America/Lima`) y `book_appointment.php` con **anti-doble-booking transaccional (409 Conflict verificado)**. Isla `<AppointmentPicker>` integrada. | Confirmación de citas actualmente manual desde panel `/admin/`. | (Futuro opcional) Notificaciones automáticas por email/WhatsApp. |
| **016 (Analítica de Conversión y Loop `chat → lead → learn`)** | ✅ **DESPLEGADA Y AUDITADA EN PRODUCCIÓN (2026-08-02).** Live en subdominio `https://app.datanestiq.com`. Tabla `chat_metrics` instrumentada en `chat.php` con insert fail-safe `try/catch`. Columnas `sector` y `rol` añadidas a `leads` mediante migración idempotente preservando 100% de datos previos (con backup `crm.sqlite.bak`). Captura E2E de sector/rol verificada en producción. 3 endpoints de analítica agregados sin PII en `admin/api.php`. Pestaña de Analítica de Conversión ligera (Vanilla CSS) y visualizador de chat por `session_id` en `/admin/`. Script offline `scripts/learn_prompt_optimizer.mjs` con consultas SQL reales vía `node:sqlite` y guardarraíl de honestidad radical (§2) que emite "Datos insuficientes" sin fabricar porcentajes ficticios. | Ninguna. | Producción activa y monitoreada. |
| **017 (Panel de Observabilidad Interna - Ops & Specs)** | 🟠 **DISEÑADA, PENDIENTE DE AUDITORÍA (2026-08-02).** `specs/017-panel-observabilidad/spec.md`. Artefactos de diseño SDD creados (`spec.md`, `data-model.md`, `plan.md`, `tech_debt.md`). Telemetría Ops (salud de failover, latencias p50/p95, tendencias desde `chat_metrics`) y SSOT estructurado del estado de specs en `src/data/specsStatus.json` (0 PII, detras de `auth.php`). En espera de auditoría por Claude Code antes de implementar runtime. | Muestra inicial en frío para p50/p95. | Esperar auditoría y luz verde de Claude Code. |

### ⚠️ Pendientes Transversales e Higiene Técnica General
- **Honestidad Radical (Constitución §2):** Re-alineada la página `/casos-de-exito` — disclaimer transparente aclarando que son escenarios **ilustrativos** (no clientes reales anonimizados), eliminación del framing engañoso de "confidencialidad", adición de badges **`[EST]`** en las 6 cifras de métricas, y CTA honesto ("Diseñemos juntos su próxima arquitectura de valor").
- **Seguridad:** El código ya no expone credenciales (`remote_extract.py` lee de `.env`, gitignored). **PENDIENTE CRÍTICO (solo el usuario):** rotar la contraseña SSH de IONOS, que quedó en el historial de git.
- **CRM/Agendador (Specs 014/015) no-live:** su puesta en producción está **encadenada al despliegue a IONOS** (`planes/DESPLIEGUE-IONOS.md`), que a su vez depende del Paso 0 de credenciales SSH (arriba). Una vez desplegado: poblar el `.env` de prod (`ADMIN_PASSWORD_HASH` + `ALLOWED_IPS`) y correr `init_crm_db.php` + `migrate_leads.php` en el servidor.
- **Testing Continuo:** Los scripts de validación ya usan **runners reales** (eslint + `@lhci/cli`, ya no `echo`). Pendiente: limpiar el baseline de lint (149e/130w) y correr Lighthouse en un entorno sin el error `EPERM` de Windows.
- **Operatividad Documental:** El flujo OpenWiki evolucionó al generador multi-destino de la Spec 010, que ahora fundamenta la documentación wiki, de agentes, skills y blogs utilizando un balanceador de carga ponderado.
