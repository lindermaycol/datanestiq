# Estado de las Especificaciones (Specs)

A continuación se resume el estado real, sincronizado y auditado de las 13 especificaciones arquitectónicas del proyecto Datanestiq.

| Spec | Fase real | Deuda abierta principal | Próximo hito |
|------|-----------|-------------------------|--------------|
| **001 (Elevación Premium)** | Implementado en Astro (producción) | Componentes UI en Vanilla, dependencias Tailwind pendientes de compilación completa. | Refactor nativo a Astro Components (`<Button>`, `<Card>`). |
| **002 (Microexperiencias IA)** | ✅ Consume la taxonomía enriquecida en las 5 islas (chatbot guiado **0 llamadas**, `problems` conservado). **Chatbot texto-libre resiliente**: `chat.php` con **failover Groq→DashScope→Gemini** (verificado en vivo: Groq 401→responde por DashScope, sin error) + **grounded en servicios** (recomienda/enlaza `/soluciones/[slug]` desde `services.json`). | Afinamiento de umbrales del Buscador Semántico y streaming SSE. | Refinar threshold NLP. |
| **003 (Taxonomía Servicios)** | ✅ Content Collections validadas + 10 páginas sector. **+ Fundación de profundidad AUDITADA:** `/soluciones/*` y `/sectores/*` renderizan los campos enriquecidos (techStack, competitivePositioning, proofPoints, KPIs `[EST]`, deploymentModels/engagementModels) + bloque **"Resolvemos tus dudas"** (objeción→respuesta) en las 16 páginas vía `pillarsOfInterest`/`relevantPersonas`. Schema ampliado: `objectionResponses`, `deploymentModels`, `engagementModels`, `roiCases`. | Múltiples fuentes unificadas vía pre-build JSON | Expandir a otras entidades de negocio si aplica. |
| **004 (Metodología)** | 🔴 **ABANDONADA** (decisión del usuario): el pipeline LangGraph en Python (`backend/langgraph_pipeline.py`) nunca existió y no se construirá. | El OBJETIVO (agentes que redacten/mantengan el sitio) NO se abandona — se sirve con el patrón **controlado single-shot** de la Spec 010 (`docs-generator.mjs`), no con agentes multi-turno frágiles. | Migrar la intención a la "fábrica de contenido del sitio" sobre la Spec 010 (nueva spec). |
| **005 (OpenWiki Privada)** | ✅ CONSOLIDADA en Spec 010 (su motor propio evolucionó a `docs-generator.mjs`, target `wiki`) — NO archivada como fallida | Ninguna (deuda migrada a la 010) | Cerrada por superseded; ver Spec 010. |
| **006 (Ecosistema Astro)** | ✅ Implementado + **tooling de validación REAL** (eslint + `@lhci/cli` instalados, ya no son stubs `echo`). | Baseline ESLint = 149 errores/130 warnings por limpiar (no bloqueante). Lighthouse configurado pero **no produjo scores** (error `EPERM` de Windows en Temp). | Limpiar deuda de lint; correr Lighthouse en entorno sin EPERM. |
| **007 (Expansión Multi-Página)**| Implementado + **Breadcrumbs Schema JSON-LD** (BreadcrumbList) en BaseLayout. **+ Fundación AUDITADA:** hubs `/soluciones` y `/sectores` (dinámicos, en nav desktop+mobile y sitemap) + páginas legales `/privacidad` y `/terminos` (con `[BORRADOR LEGAL]`; **cero `href="#"`** en footer). | Falta la **auditoría Lighthouse real** (bloqueada por `EPERM` de Windows; el tooling ya existe). | Correr Lighthouse en entorno sin EPERM y reportar scores. |
| **008 (Headless WordPress)** | ⏸️ En pausa por decisión del usuario. Código ya remediado: `remote_extract.py` lee de `.env` y está gitignored. | 🔴 **CRÍTICO (acción del usuario):** rotar la contraseña SSH de IONOS — sigue expuesta en el historial de git. Falta CPTs y API REST. | Rotar SSH en IONOS; configurar endpoints WP cuando se retome. |
| **009 (OpenWiki LangChain)** | 🔴 [ARCHIVADA] Bloqueada por cuota de free tier. | Agotaba limits por el flujo del agente | Pivotó a Spec 010 (Generador Multi-Destino) |
| **010 (Generador Multi-Destino)** | 🟢 Operativo: 4 targets (wiki/agents/skills/blog), seguridad git+lista, backoff 429, poolIndex aleatorio. Correcciones de auditoría aplicadas y verificadas (build **23 páginas**, `skills-overview.md` + blogs OK). | **Higiene SDD:** faltan `plan.md` y `tasks.md` formales en `specs/010/` (el plan quedó en `/planes`). (Rama del bot `bot/openwiki-sync` es cosmética.) | Formalizar `plan.md`/`tasks.md` de la 010; luego configurar ejecución programada. |
| **011 (Enriquecimiento Taxonomía)** | ✅ **Fase 2 completa y AUDITADA en caliente**: 10/10 sectores, 6/6 pilares, 7 personas con contenido **específico por industria** (regulaciones reales: HIPAA/Basilea/SBS/FERPA/ESG…), integridad de aristas (exit 1 verificado), corpus liviano (16K/24K, content-angles separados=23), 3 posts de blog de sectores distintos. Build 24 páginas. | ✅ **Fase 3 COMPLETA y AUDITADA** (`prompts/prompt-antigravity-spec011-fase3-enriquecimiento-conversion.md`): pilares con arquitectura/despliegue/integración/TCO + catálogo/linaje/stewardship; KPIs `[EST]` 4–6 en financieros; `objectionResponses` **1:1 con `objections[]` (cero fallbacks genéricos, verificado)**; `deploymentModels`/`engagementModels` del público; `roiCases`. `build-taxonomy` verde (Zod + aristas). Sin datos fabricados. | Menor (preexistente): `personas.json` aún envía `contentAngles` al cliente. | ✅ Pipeline completo (F3 → fundación → Spec 013). |
| **013 (Conversión Consultiva por Rol × Sector)** | ✅ **COMPLETA y AUDITADA en navegador (spec-marco + 3 fases).** `specs/013-.../spec.md`. **Motor A1–A9:** `userContext` store persistido, `<ContextChips>` (chips de hero descartables), `<ConsultativeCTA>` dinámico (verificado: chip Finanzas→"Auditoría de ROI…"), reuso de `semanticHighlight` (sin motor paralelo), formulario vía `open-chatbot`→`chat.php` (**sin `contact.php`/`remote_extract`**, PII segura), `<IllustrativeRoiCase>` con badge `[EST] Escenario Ilustrativo`. **Fase 1 Público:** bloque seguridad On-Prem/VPC condicional, intents institucionales, FAQ, blogs. **Fase 2 CFO:** intent 0-LLM "EBITDA/TCO" (verificado en Network: cero `chat.php`), bloque coexistencia-ERP (consume `objectionResponses`), **Business Case Estimator** (`/business-case`, cálculo client-side, `[EST]`, opt-in→`save_wizard.php`). **Fase 3 CEO:** intent "ventaja competitiva", Perspectiva Estratégica, CTA/grilla contextual. 0-LLM preservado, build verde (35 págs), consola limpia. | ✅ **Modelo del Business Case Estimator REFINADO y AUDITADO en navegador:** fracción direccionable (slider 25%), horizonte 3 años con ramp 50% año 1, costo de implementación escalado (`max(40k, costBase*0.05)`), uplift de ingresos muy conservador. Verificado en vivo: mid-market **144% ROI / 7.7 meses** (antes 1775%/0.5mo); SMB muestra honestamente **"N/A (ROI Negativo) / >36 meses"**. `[EST]` + client-side + opt-in intactos. Blogs/FAQ ampliables. | Sumar roles nuevos (CISO/COO) como fases del motor 013 cuando lleguen sus insumos. |
| **012 (Fábrica de Contenido del Sitio)** | ✅ **Fase 1 + Fase 2 operativas y AUDITADAS**. F1: colección `pages` (Zod+`loader:glob`), catch-all `[...slug].astro`, `--target=page` (draft-gating verificado). F2: **home data-driven** (`home.md`→`index.astro` vía `getEntry`+defaults, `set:html` correcto, **paridad verificada** tras fix `\n`→`<br/>`); **sin `/home` duplicada**; 5 tipos de sección nuevos; workflow `content-pr.yml` (secrets + PR, sin auto-merge). Islas intactas, build 25 páginas. | Menor: `pagina-muestra.md` (demo F1) sigue publicada. | Fase 3 opcional: proponer cambios de home vía el flujo PR; más rollout. |

### ⚠️ Pendientes Transversales (Higiene Técnica General)
- **Seguridad:** El código ya no expone credenciales (`remote_extract.py` lee de `.env`, gitignored). **PENDIENTE CRÍTICO (solo el usuario):** rotar la contraseña SSH de IONOS, que quedó en el historial de git.
- **Testing Continuo:** Los scripts de validación ya usan **runners reales** (eslint + `@lhci/cli`, ya no `echo`). Pendiente: limpiar el baseline de lint (149e/130w) y correr Lighthouse en un entorno sin el error `EPERM` de Windows.
- **Operatividad Documental:** El flujo OpenWiki evolucionó al generador multi-destino de la Spec 010, que ahora fundamenta la documentación wiki, de agentes, skills y blogs utilizando un balanceador de carga ponderado.
